# log level
debug: true
# agent token
token:

# shortcut convert a single word to shell command
commands:
  - msg: free
    cmd: free
  - msg: date
    cmd: date
  - msg: root
    cmd: ls
    dir: /

# template use printf style, this is unsafe!
templates:
  - name: echo
    temp: echo %s